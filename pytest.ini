[pytest]
markers =
    unit: Fast, isolated tests targeting single functions
    integration: Slower tests targeting cross-component orchestration

testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*

# Force tests to be run from the root directory so imports work
pythonpath = .
